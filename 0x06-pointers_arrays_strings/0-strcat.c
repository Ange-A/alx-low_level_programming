#include "main.h"

/**
 * _strcat - concatenates two strings
 * @dest: string to append to
 * @src: string to add
 * @n: the number of bytes that will be used from src
 * Return: a pointer to the resulting string
 */
char *_strcat(char* dest, char *src)
{
	int i, j;
	
	i = 0;
        j = 0;

        while (x = 0; dest[i] != '\0'; i++)

        while (x= 0; src[j] != '\0' && n > 0; j++, n--, i++)
        {
               dest[i] = src[j];
               
        }

        return (dest);
}
